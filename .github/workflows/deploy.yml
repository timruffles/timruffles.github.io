name: Deploy 🚀

on:
  push:
    branches: ["master", "deploy-test"]
  issue_comment:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - uses: actions/checkout@v2
      - run: mkdir gh-pages
      - run: npm install --omit=dev
      - run: "./node_modules/.bin/ts-node ./list-articles > ${{env.GITHUB_WORKSPACE}}/site.json"
      - uses: ./.github/actions/issue-comments
        with:
          content-path: "${{env.GITHUB_WORKSPACE}}/site.json"
      - run: BASE_URL="/" npm run -s build
      - uses: peaceiris/actions-gh-pages@v3.7.3
        with:
          deploy_key: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: gh-pages
